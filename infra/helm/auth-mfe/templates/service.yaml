apiVersion: v1
kind: Service
metadata:
  name: {{ include "auth-mfe.fullname" . }}
  labels:
    {{- include "auth-mfe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "auth-mfe.selectorLabels" . | nindent 4 }}
