apiVersion: v1
kind: Service
metadata:
  name: agent-orchestrator
  namespace: serphona
  labels:
    app: agent-orchestrator
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: agent-orchestrator
