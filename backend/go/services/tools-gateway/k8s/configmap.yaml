apiVersion: v1
kind: ConfigMap
metadata:
  name: tools-gateway-config
  namespace: serphona
data:
  # Observability
  jaeger-endpoint: "http://jaeger-collector:14268/api/traces"
  
  # App Config
  log-level: "info"
  timeout: "30s"
  max-connections: "100"
