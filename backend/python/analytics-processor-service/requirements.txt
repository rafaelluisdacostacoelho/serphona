fastapi>=0.95.0
uvicorn[standard]>=0.22.0
aiokafka>=0.8.0
clickhouse-connect>=0.6.0
pydantic>=1.10.0
prometheus-client>=0.15.0
tenacity>=8.0.0
# Optional (heavy) dependencies for on-device inference — install only if needed
# transformers>=4.40.0
# torch>=2.0.0
